#include <iostream>
#include <iomanip>
#include <sstream>
#include <array>
#include <cstdint>

using namespace std;
using Block = array<uint8_t, 16>;
using Key = array<uint8_t, 16>;
using RoundKeys = array<uint8_t, 176>; // AES-128: 11 轮 * 16字节 = 176字节

//S盒
static const uint8_t sbox[256] = {

};

//有限域内字节乘法改
uint8_t xtime(uint8_t x) {
    // xtime(x) = x * 2 in G(2^8)
    return (x << 1) ^ ((x & 0x80) ? 0x1b : 0x00);
}

uint8_t mul(uint8_t x, uint8_t y) {
    // G(2^8) 域内的乘法
    uint8_t r = 0;
    while (y) {
        if (y & 1) r ^= x;
        x = xtime(x);
        y >>= 1;
    }
    return r;
}

// 密钥扩展函数
void KeyExpansion(const Key& key, RoundKeys& roundKeys) {
    static const uint8_t Rcon[10] = {
        0x01, 0x02, 0x04, 0x08, 0x10,
        0x20, 0x40, 0x80, 0x1B, 0x36
    };

    for (int i = 0; i < 16; ++i)
        roundKeys[i] = key[i];

    uint8_t temp[4];
    int i = 16;
    int rconIndex = 0;

    while (i < 176) {
        for (int j = 0; j < 4; ++j)
            temp[j] = roundKeys[i - 4 + j];

        if (i % 16 == 0) {
            // 循环移位
            uint8_t t = temp[0];
            temp[0] = temp[1];
            temp[1] = temp[2];
            temp[2] = temp[3];
            temp[3] = t;
            //S盒替换
            for (int j = 0; j < 4; ++j)
                temp[j] = sbox[temp[j]];
            // 与Rcon异或
            temp[0] ^= Rcon[rconIndex++];
        }

        for (int j = 0; j < 4; ++j) {
            roundKeys[i] = roundKeys[i - 16] ^ temp[j];
            ++i;
        }
    }
}

//状态操作函数,S盒替换
void SubBytes(Block& state) {
    for (auto& b : state)
        b = sbox[b];
}

void ShiftRows(Block& state) {
    // Row 1:（就地移位，避免不必要的复制开销）
    uint8_t temp1 = state[1];
    state[1] = state[5];
    state[5] = state[9];
    state[9] = state[13];
    state[13] = temp1;

    // Row 2:
    uint8_t temp2 = state[2], temp6 = state[6];
    state[2] = state[10];
    state[6] = state[14];
    state[10] = temp2;
    state[14] = temp6;

    // Row 3: 
    uint8_t temp15 = state[15];
    state[15] = state[11];
    state[11] = state[7];
    state[7] = state[3];
    state[3] = temp15;
}

//列混淆函数
void MixColumns(Block& state) {
    for (int i = 0; i < 4; ++i) {
        int col = i * 4;
        uint8_t a = state[col];
        uint8_t b = state[col + 1];
        uint8_t c = state[col + 2];
        uint8_t d = state[col + 3];

        state[col] = mul(a, 2) ^ mul(b, 3) ^ c ^ d;
        state[col + 1] = a ^ mul(b, 2) ^ mul(c, 3) ^ d;
        state[col + 2] = a ^ b ^ mul(c, 2) ^ mul(d, 3);
        state[col + 3] = mul(a, 3) ^ b ^ c ^ mul(d, 2);
    }
}
//轮密钥加函数
void AddRoundKey(Block& state, const RoundKeys& roundKeys, int round) {
    for (int i = 0; i < 16; ++i)
        state[i] ^= roundKeys[round * 16 + i];
}

//AES 加密主函数
void AES_encrypt(Block& input, const Key& key) {
    RoundKeys roundKeys;
    KeyExpansion(key, roundKeys);

    AddRoundKey(input, roundKeys, 0);

    for (int round = 1; round < 10; ++round) {
        SubBytes(input);
        ShiftRows(input);
        MixColumns(input);
        AddRoundKey(input, roundKeys, round);
    }
    // 最后一轮不进行列混淆
    SubBytes(input);
    ShiftRows(input);
    AddRoundKey(input, roundKeys, 10);
}
// 解析16字节16进制字符串为Block
Block hexstr_to_block(const string& hex) {
    Block b = {};
    for (int i = 0; i < 16; ++i) {
        string byte = hex.substr(i * 2, 2);
        b[i] = static_cast<uint8_t>(stoi(byte, nullptr, 16));
    }
    return b;
}


// 主函数
int main() {
    string key_str, pt_str;
    //cout << "Input:(Key & Plaintext 小写 16 进制)" << endl;
    cin >> key_str;
    cin >> pt_str;

    Key key = hexstr_to_block(key_str);
    Block plaintext = hexstr_to_block(pt_str);

    AES_encrypt(plaintext, key);

    //cout << "Output: （Ciphertext 大写 16 进制）" << endl;
    for (size_t i = 0; i < plaintext.size(); ++i)
        printf("%02X", plaintext[i]);


    return 0;
}
